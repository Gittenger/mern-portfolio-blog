import styled from 'styled-components'

import { cssIndex } from '../../styles/css/utils.styles.js'

export const EditPostsContainer = styled.div``

export const Row = styled.div`
	${cssIndex.flexCenter}

	p {
		margin-bottom: 0;
	}

	button {
		margin-left: 0.7rem;
		cursor: pointer;
	}
`
